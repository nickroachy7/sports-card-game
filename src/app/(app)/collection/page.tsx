import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function CollectionPage() {
  return (
    <PagePlaceholder
      title="Collection"
      phase="Phase 1 (M5)"
      description="Card browser with filters (position, tier, status, contract, token) and search. Lands in M5."
    />
  );
}
