import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function MilestonesPage() {
  return (
    <PagePlaceholder
      title="Milestones"
      phase="Phase 4"
      description="Team milestones dashboard: progress bars for hits, HR, SB, wins, plus a recap feed."
    />
  );
}
