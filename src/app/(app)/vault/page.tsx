import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function VaultPage() {
  return (
    <PagePlaceholder
      title="Vault"
      phase="Phase 4"
      description="Permanent, season-scoped keepsake showcase. Lands in Phase 4 with the vault ceremony."
    />
  );
}
